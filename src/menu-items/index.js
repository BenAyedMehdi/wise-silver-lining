import spending from './dashboard';
import childAccounts from './pages';
import track from './track';

// ==============================|| MENU ITEMS ||============================== //

const menuItems = {
    items: [spending, track, childAccounts]
};

export default menuItems;
